# cohere_beamlines

This repository contains modules implementing beamline specific code that depends on instrument used during experiment.
It is written to the interface in cohere_ui. Each supported beamline is a module recognized by directory name reflecting that beamline.
This repository also keeps correction files for supported beamlines.
It is updated with a new event, such as new darkfield or whitefield file.